import { EMPTY, HUM, COMPUTE, WALL, MAX, MIN } from './constant';
import getDurationList from './getDurationList';
// import getScore from './getScore';

/**
 * 计算当前位置得分
 */

//  const { list, index,  type, chessPlayer } = params;
const getScoreByPosition = params => {
  // const { chessPlayer } = params;
  // return getScore(getDurationList(params), chessPlayer);
};

export default getScoreByPosition;
